# Academy / open-wheel body

- `academy-body.glb` — baked loft hull + sidepods + cover + fin + floor.
  Regenerate: `node scripts/build-academy-car-glb.mjs`
- Loaded by `F1CarMesh` via Drei `useGLTF`; procedural loft fallback on failure.
